{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities",
  "windows": ["main", "oauth-login", "oauth-callback"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "http:default",
    "http:allow-fetch"
  ]
}
